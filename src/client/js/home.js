const postContainer = document.querySelector(".postContainer");

const handleMain = (e) => {
    if (e.target === e.currentTarget) {
        return;
    }

};
postContainer.addEventListener("click", handleMain);