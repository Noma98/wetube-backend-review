header
    if loggedIn
        if loggedInUser.avatarUrl 
            img(src=loggedInUser.avatarUrl) 
        else 
            span 👩‍🦰
        p 안녕하세요, #{loggedInUser.name}님!😁
    nav 
        ul 
            li 
                a(href="/") home 
            if !loggedIn
                li 
                    a(href="/login") Login
                li 
                    a(href="/join") Join
            else 
                li 
                    a(href="/logout") Logout